<section class="page">
  <div class="centered-box">
    <h1>Update <%= @cat.name %></h1>
    
    <%= render "shared/errors" %>
    <%= render 'form', cat: @cat %>
  </div>
</section>

<% content_for :footer do %>
  <p>This is the edit page for <%= @cat.name %>.</p>
<% end %>